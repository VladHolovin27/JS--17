 let user = {
 height: 20,
 width: 30
 };
function userResult() {
    let result = height + width;
    console.log(result);
}


function jgo(callback1, callback, number)
   if (number > 10) {
    callback1();
   else {
    callback();
    }
}
 jgo((callback1) => {Vlad}, (callback) => {Artem});